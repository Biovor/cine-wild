<form method="post" action="{{ path('public_age_delete', {'id': public_age.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ public_age.id) }}">
    <button class="btn">Delete</button>
</form>
{% extends 'base.html.twig' %}

{% block title %}TypeFilms{% endblock %}

{% block body %}
    <h1>TypeFilms</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_film.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ type_film.label }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_films_index') }}">back to list</a>

    <a href="{{ path('type_films_edit', {'id': type_film.id}) }}">edit</a>

    {{ include('type_films/_delete_form.html.twig') }}
{% endblock %}
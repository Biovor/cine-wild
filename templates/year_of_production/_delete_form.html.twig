<form method="post" action="{{ path('year_of_production_delete', {'id': year_of_production.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ year_of_production.id) }}">
    <button class="btn">Delete</button>
</form>
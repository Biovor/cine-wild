{% extends 'base.html.twig' %}

{% block title %}YearOfProduction{% endblock %}

{% block body %}
    <h1>YearOfProduction</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ year_of_production.id }}</td>
            </tr>
            <tr>
                <th>Year</th>
                <td>{{ year_of_production.year }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('year_of_production_index') }}">back to list</a>

    <a href="{{ path('year_of_production_edit', {'id': year_of_production.id}) }}">edit</a>

    {{ include('year_of_production/_delete_form.html.twig') }}
{% endblock %}